plugins {
    id 'java-library'
}

dependencies {
    api(project(":wizard-api"))
    api 'org.springframework.boot:spring-boot-starter-data-jpa'
}

jar {
    enabled = true
    archiveClassifier = ''
}